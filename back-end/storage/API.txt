Pages are stored in the db (not documents).
Each page is stored using a ID (unique).
//TO DO
Now the id is just the hash oh the doc. The storing method has to be improved


1) GET /document   params = { idDOC }   return application/pdf
   
   404 Not found => The doc doesn't exist

The server asks the metadataServer the pages composing the documents. Then it puts all the pages together
and returns the pdf document.

2) POST /document  params = { doc (application/pdf) } return application/json

   If all the pages are original the response contains "document".

   Otherwise it is "mash-up".
   
E LA RICERCA BRUTTI CANI?

Mi servono API per:
                                      TIPO (da client a server)
1) ricerca di un documento                    GET
2) upload di un documento                     POST
3) download di un documento                   GET
4) autenticazione di un utente                POST
5) registrazione di un nuovo utente           POST


